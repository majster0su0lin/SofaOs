[bits 16]
[org 0x1000]
start:
   xor ax, ax
   mov ds, ax
   mov es, ax
   cmp byte [isbsp], 1
   je apentry
   mov byte [isbsp], 1
   mov [disk], dl
   mov [pamet], bx
   mov [pamet2], cx
   mov ah, 0x0E
   mov al, 'K'
   int 0x10
   mov al, 0x0D
   int 0x10
   mov al, 0x0A
   int 0x10
   mov si, bbios
   call cpirnt
   cli
   lgdt [gdtp]
   mov eax, cr0
   or eax, 1
   mov cr0, eax
   jmp CIDE_SEG:fnl32b
cpirnt:
   mov ah, 0x0E
lp:
   lodsb
   test al, al
   jz kn
   int 0x10
   jmp lp
isbsp: db 0
kn:
   ret
align 4
gdts:
dd 0, 0
gdtc:
dw 0xFFFF
dw 0x0000
db 0x00
db 0x9A
db 0xCF
db 0x00
gdtd:
dw 0xFFFF
dw 0x0000
db 0x00
db 0x92
db 0xCF
db 0x00
gdt67:
dw 0x0000
dw 0x0000
db 0x00
db 0x9A
db 0x20
db 0x00
gdte:
gdtp:
   dw gdte - gdts -1
   dd gdts
CIDE_SEG equ gdtc -gdts
DATA_SEG equ gdtd - gdts
CODE67_SEG equ gdt67 - gdts
disk: db 0
bbios: db "bye bye bios forever", 0x0D, 0x0A, 0
pamet: dw 0
pamet2: dw 0
apentry:
   mov al, 67
   mov byte [0x67], al
   hlt
   jmp apentry
fnl32b:
[bits 32]
   mov eax, cr0
   and eax, 0xFFFFFFFB
   or  eax, 0x60000000
   and eax, ~(1 << 2)
   or  eax, (1 << 5)
   mov cr0, eax
   mov eax, cr4
   or  eax, (1 << 9) | (1 << 10)
   mov cr4, eax
   mov byte [0xb800c], 'F'
   mov byte [0xb800d], 0x0F
   mov ax, DATA_SEG
   mov ds, ax
   mov es, ax
   mov fs, ax
   mov gs, ax
   mov ss, ax
   mov esp, 0x67000
   mov byte [0xB8000], 'p'
   mov byte [0xB8001], 0x0
   mov edi, 0x70000
   xor eax, eax
   mov ecx, 10240
   rep stosd
   mov dword [0x70000], 0x71003
   mov dword [0x70004], 0x00000000
   mov edi, 0x71000
   mov eax, 0x72003
   mov ecx, 8
loo:
   mov [edi], eax
   mov dword [edi+4], 0
   add edi, 8
   add eax, 0x1000
   loop loo
   mov edi, 0x72000
   mov eax, 0x00000083
   mov edx, 0
   mov ecx, 4096
looo:
   mov [edi], eax
   mov [edi+4], edx
   cmp eax, 0xFEC00083
   jb skipi
   or dword [edi], 0x10
skipi:
   add edi, 8
   add eax, 0x00200000
   adc edx, 0
   loop looo
   mov eax, cr4
   or eax, 1 << 5
   mov cr4, eax
   mov eax, 0x70000
   mov cr3, eax
   mov ecx, 0xC0000080
   rdmsr
   or eax, 1 << 8
   wrmsr
   mov eax, cr0
   or eax, 1 << 31
   mov cr0, eax
   jmp CODE67_SEG:epstien
[bits 64]
epstien:
   mov byte [0xb8000], 'e'
   mov byte [0xb8001], 0x07
   xor al, al
   xor dl, dl
   xor r15, r15
   xor cl, cl
   xor bl, bl
   xor r9, r9
   mov r9b, 1
   mov r12, hlpg
strg:
   mov byte [0xb8000], 'q'
   mov byte [0xb8001], 0x7
   xor r11d, r11d
   xor r8, r8
   movzx r10d, r9b
   shl r10d, 31
   movzx r8d, al
   shl r8d, 16
   or r8d, r10d
   xor r10, r10
   movzx r10, bl
   shl r10d, 11
   or r8d, r10d
   xor r10, r10
   movzx r10d, cl
   shl r10d, 8
   or r8d, r10d
   xor r10, r10
   mov r13b, al
   xor rax,rax
   mov dx, 0xcf8
   mov eax, r8d
   out dx, eax
   xor rax, rax
   mov dx, 0xcfc
   in eax, dx
   mov r11d, eax
   xor rax, rax
   mov al, r13b
   add cl, 1
   cmp cl, 8
   je tobl
bc:
   cmp bl, 32
   je toal
ac:
   cmp r11d, 0xffffffff
   je strg
   mov [r12], r8d
   add r12, 4
   mov [r12], r11d
   jmp ttwo
tt:
   add r12, 4
   mov byte [0xb8002], 'd'
   mov byte [0xb8003], 0x7
   add r15, 1
   jmp strg
tobl:
   xor cl, cl
   add bl, 1
   jmp bc
toal:
   cmp al, 255
   je steb
   xor bl, bl
   add al, 1
   jmp ac
steb:
   mov rax, [savior]
   and rax, 0xFFFFFFFF0
   mov eax, [rax + 0x0C]
   xor rbx, rbx
bitest:
   bt eax, ebx
   jc yez
   add rbx, 1
   cmp rbx, 32
   je konc
   jmp bitest
pbs: dq 0
npo: db 'no device found', 0
yez:
   mov edx, ebx
   shl rdx, 7
   mov rcx, [savior]
   add rcx, 0x100
   add rdx, rcx
   add rdx, 0x28
   mov ecx, [rdx]
   and ecx, 0xf
   cmp ecx, 3
   je yess
   add rbx, 1
   jmp bitest
konc:
   mov rdx, npo
   mov cl, 0x7
   call cpritn
   ret
yess:
   sub rdx, 0x28
   mov [pbs], rdx
   mov byte [0xb8002], 'd'
   mov byte [0xb8003], 0x07
   mov qword [count], r15
   mov rax, [pbs]
   add rax, 0x18
   mov ebx, [rax]
   shr ebx, 1
   shl ebx, 1
   mov [rax], ebx
walt:
   mov ebx, [rax]
   bt ebx, 15
   jc walt
   mov ebx, [rax]
   btr ebx, 4
   mov [rax], ebx
aaah:
   mov ebx, [rax]
   bt ebx, 14
   jc aaah
   mov rax, 0x79fff
zot:
   add rax, 1
   mov byte [rax], 0
   cmp rax, 0x7a4ff
   jne zot
   mov rax, [pbs]
   mov rbx, rax
   mov dword [rax], 0x7a000
   add rax, 0x4
   mov dword [rax], 0
   add rax, 0x4
   mov dword [rax], 0x7a400
   add rbx, 0xc
   mov dword [rbx], 0
   sub rbx, 0xc
   add rbx, 0x18
   mov ecx, [rbx]
   mov edx, 1
   shl edx, 4
   or ecx, edx
   mov [rbx], ecx
   mov byte [17], 69
   xor rax, rax
   mov byte [0xb8004], '1'
   mov byte [0xb8005], 0x07
   call drs
   jmp miguel
drs:
   mov byte [0x7a000], 5
   mov byte [0x7a001], 0
   mov word [0x7a002], 1
   mov dword [0x7a004], 0
   mov dword [0x7a008], 0x7a500
   mov dword [0x7a00c], 0
   mov rdx, 0x7a500
zrr:
   mov byte [rdx], 0
   cmp rdx, 0x7a590
   je zre
   add rdx, 1
   jmp zrr
   mov byte [0xb8008], '3'
   mov byte [0xb8009], 0x07
zre:
   mov byte [0x7a500], 0x27
   mov byte [0x7a501], 0x80
   mov byte [0x7a502], 0x25
   mov byte [0x7a507], 0x40
   mov byte [0x7a504], al
   shr rax, 8
   mov byte [0x7a505], al
   shr rax, 8
   mov byte [0x7a506], al
   shr rax, 8
   mov byte [0x7a508], al
   shr rax, 8
   mov byte [0x7a509], al
   shr rax, 8
   mov byte [0x7a50a], al
   mov byte [0x7a50c], 1
   mov byte [0x7a50d], 0
   mov dword [0x7a580], 0x7b000
   mov dword [0x7a584], 0
   mov dword [0x7a588], 0
   mov eax, 511
   mov ebx, 1
   shl ebx, 31
   or eax, ebx
   mov dword [0x7a58c], eax
   mov rax, [pbs]
   mov rbx, rax
   add rbx, 0x38
   add rax, 0x18
   mov edx, [rax]
   bts edx, 0
   mov [rax], edx
   mov ecx, 1
   mov dword [rbx], ecx
aah_i_have_an_head_ache_from_writing_this_shit:
   mov ecx, [rbx]
   bt ecx, 0
   jc aah_i_have_an_head_ache_from_writing_this_shit
   ret
tte:
   pop rax
   pop rbx
   jmp tt
ttwo:
   mov r14d, 0x8
   push rbx
   mov ebx, r8d
   or r8d, r14d
   push rax
   xor rax, rax
   mov eax, r8d
   mov dx, 0xcf8
   out dx, eax
   mov dx, 0xcfc
   in eax, dx
   mov r14d, eax
   shr eax, 24
   cmp eax, 1
   jne tte
   mov eax, r14d
   shr eax, 16
   and eax, 0xff
   cmp eax, 0x6
   jne tte
   mov eax, r14d
   shl eax, 16
   shr eax, 24
   cmp eax, 0x1
   jne tte
   mov r14d, 0x24
   mov r15d, 0x4
   mov ecx, ebx
   or ecx, r15d
   mov dx, 0xcfc
   mov eax, ecx
   out dx, eax
   mov dx, 0xcfc
   in eax, dx
   or ecx, 0b110
   mov dx, 0xcfc
   mov eax, ecx
   out dx, eax
   mov eax, ebx
   or eax, r14d
   mov dx, 0xcf8
   out dx, eax
   mov dx, 0xcfc
   in eax, dx
   mov [savior], eax
   pop rax
   pop rbx
   mov byte [16], 67
   jmp steb
savior: dq 0
count: dq 0
hexbintrn:
   mov byte [netanyahu], 48
   mov byte [netanyahu + 1], 'x'
   mov byte [netanyahu + 19], 0
   mov r8, 18
   mov r9, rax
   mov rbx, 16
hexbenjamin:
   xor rax, rax
   mov rax, r9
   xor rdx, rdx
   div rbx
   mov r9, rax
   mov rax, rdx
   sub r8, 1
   cmp rax, 10
   je jea
   cmp rax, 11
   je jeb
   cmp rax, 12
   je jec
   cmp rax, 13
   je jed
   cmp rax, 14
   je jee
   cmp rax, 15
   je jef
   add rax, 48
hxlp:
   mov byte [netanyahu + r8], al
   cmp r8, 2
   jne hexbenjamin
   ret
jea:
   mov al, 'A'
   jmp hxlp
jeb:
   mov al, 'B'
   jmp hxlp
jec:
   mov al, 'C'
   jmp hxlp
jed:
   mov al, 'D'
   jmp hxlp
jee:
   mov al, 'E'
   jmp hxlp
jef:
   mov al, 'F'
   jmp hxlp
bintrn:
   mov rbx, 10
   xor rcx, rcx
   mov r8, netanyahu
   mov r9, rax
idf:
   xor rdx, rdx
   add rcx, 1
   div rbx
   cmp rax, 0
   jne idf
   add r8, rcx
   mov [r8], rax
   mov rax, r9
benjamin:
   xor rdx, rdx
   sub r8, 1
   div rbx
   add rdx, 48
   mov byte [r8], dl
   cmp rax, 0
   jne benjamin
   ret
netanyahu: times 22 db 0
ksnc:
   push rax
   push rcx
   push rbx
   xor rax, rax
   in al, 0x60
   mov cl, al
   mov al, 0x20
   out 0x20, al
   mov al, cl
   cmp rax, 0
   je err
   movzx rbx, byte [spcbf]
   cmp rbx, 1
   je spcs
   cmp rax, 0xE0
   je special
   cmp rax, 0x1C
   je ent
   cmp rax, 10
   jle num
   cmp rax, 11
   je zer
   cmp rax, 0x0C
   je min
   cmp rax, 0x0D
   je eql
   cmp rax, 0x0E
   je del
   cmp rax, 0x39
   je spc
   cmp rax, 0x2A
   je shft
letters:
   movzx rbx, byte [shift]
   cmp rax, 0x1E
   je keya
   cmp rax, 0x30
   je keyb
   cmp rax, 0x2E
   je keyc
   cmp rax, 0x20
   je keyd
   cmp rax, 0x12
   je keye
   cmp rax, 0x21
   je keyf
   cmp rax, 0x22
   je keyg
   cmp rax, 0x23
   je keyh
   cmp rax, 0x17
   je keyi
   cmp rax, 0x24
   je keyj
   cmp rax, 0x25
   je keyk
   cmp rax, 0x26
   je keyl
   cmp rax, 0x32
   je keym
   cmp rax, 0x31
   je keyn
   cmp rax, 0x18
   je keyo
   cmp rax, 0x19
   je keyp
   cmp rax, 0x10
   je keyq
   cmp rax, 0x13
   je keyr
   cmp rax, 0x1F
   je keys
   cmp rax, 0x14
   je keyt
   cmp rax, 0x16
   je keyu
   cmp rax, 0x2F
   je keyv
   cmp rax, 0x11
   je keyw
   cmp rax, 0x2D
   je keyx
   cmp rax, 0x15
   je keyy
   cmp rax, 0x2C
   je keyz
   cmp rax, 0xAA
   je shftrel
   jmp norec
err:
   pop rbx
   pop rcx
   pop rax
   mov byte [spcbf], 0
   iretq
spcs:
   cmp rax, 0x48
   je upar
   cmp rax, 0x50
   je downar
   jmp err
upar:
   mov byte [char], '"'
   mov byte [spcbf], 0
   pop rbx
   pop rcx
   pop rax
   iretq
downar:
   mov byte [char], "'"
   mov byte [spcbf], 0
   pop rbx
   pop rcx
   pop rax
   iretq
num:
   sub rax, 1
   add rax, 48
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
zer:
   mov byte [char], 48
   pop rbx
   pop rcx
   pop rax
   iretq
min:
   mov byte [char], '-'
   pop rbx
   pop rcx
   pop rax
   iretq
eql:
   mov byte [char], '='
   pop rbx
   pop rcx
   pop rax
   iretq
del:
   xor rax, rax
   mov qword rax, [curzor]
   cmp qword rax, (0xB8000 + 480)
   je stp
   cmp byte [rax - 2], '>'
   je stp
   sub qword rax, 2
   mov byte [rax + 1], 0x70
   mov byte [rax + 3], 0x00
   mov byte [rax], 0x00
   mov qword [curzor], rax
stp:
   pop rbx
   pop rcx
   pop rax
   iretq
spc:
   mov byte [char], 32
   pop rbx
   pop rcx
   pop rax
   iretq
shft:
   mov byte [shift], 32
   pop rbx
   pop rcx
   pop rax
   iretq
shftrel:
   mov byte [shift], 0
   pop rbx
   pop rcx
   pop rax
   iretq
norec:
   pop rbx
   pop rcx
   pop rax
   iretq
keya:
   mov rax, 97
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyb:
   mov rax, 98
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyc:
   mov rax, 99
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyd:
   mov rax, 100
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keye:
   mov rax, 101
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyf:
   mov rax, 102
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyg:
   mov rax, 103
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyh:
   mov rax, 104
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyi:
   mov rax, 105
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyj:
   mov rax, 106
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyk:
   mov rax, 107
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyl:
   mov rax, 108
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keym:
   mov rax, 109
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyn:
   mov rax, 110
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyo:
   mov rax, 111
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyp:
   mov rax, 112
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyq:
   mov rax, 113
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyr:
   mov rax, 114
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keys:
   mov rax, 115
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyt:
   mov rax, 116
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyu:
   mov rax, 117
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyv:
   mov rax, 118
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyw:
   mov rax, 119
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyx:
   mov rax, 120
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyy:
   mov rax, 121
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
keyz:
   mov rax, 122
   sub rax, rbx
   mov byte [char], al
   pop rbx
   pop rcx
   pop rax
   iretq
special:
   mov byte [spcbf], 1
   pop rbx
   pop rcx
   pop rax
   iretq
spcbf: db 0
ent:
   mov byte [char], '*'
   pop rbx
   pop rcx
   pop rax
   iretq
cpritn:
   mov r14, 1
cpritnn:
   mov byte al, [rdx]
   cmp al, 0
   je ced
   mov rbx, [curzor]
   mov byte [rbx], al
   mov byte [rbx + 1], cl
   add rbx, 2
   mov [curzor], rbx
   add rdx, 1
   cmp rbx, 0xb8f9f
   jna plees
   push rcx
   push rdx
   call dnr
   pop rdx
   pop rcx
plees:
   mov r10, rcx
   mov r11, rdx
   jmp cpritnn
ced:
   xor r14, r14
   ret
cls:
   mov rax, (0xB8000 + 480)
lop:
   mov byte [rax], 0x00
   cmp rax, 0xB8FA0
   je ngeer
   add rax, 1
   jmp lop
   xor rdx, rdx
   mov rax, kniha
   mov qword [relad], rax
lup:
   mov byte [rax], 0
   add rax, 1
   add rdx, 1
   cmp rdx, 16000
   jne lup
ngeer:
   mov qword [curzor], (0xB8000 + 480)
   ret
cmper:
   xor rcx, rcx
   xor rdx, rdx
   mov byte cl, [rax]
   mov byte dl, [rbx]
   cmp dl, 0
   je sucend
   cmp cl, dl
   jne badd
   add rax, 2
   add rbx, 1
   jmp cmper
badd:
   mov rax, 0
   ret
sucend:
   mov rax, 1
   ret
cmp:
   xor rcx, rcx
   xor rdx, rdx
   mov byte cl, [rax]
   mov byte dl, [rbx]
   cmp dl, 0
   je sucendd
   cmp cl, dl
   jne baddd
   add rax, 1
   add rbx, 1
   jmp cmp
baddd:
   mov rax, 0
   ret
sucendd:
   mov rax, 1
   ret
miguel:
   xor rdx, rdx
   xor rax, rax
   mov dx, 0x3d4
   mov al, 0xa
   out dx, al
   xor rax, rax
   xor rdx, rdx
   mov dx, 0x3d5
   mov al, 0x20
   out dx, al
   call cls
   mov qword [curzor], 0xB8000
   mov rdx, head
   xor rcx, rcx
   mov cl, 0x1F
   call cpritn
   mov al, 0x11
   out 0x20, al
   out 0xA0, al
   mov al, 0x20
   out 0x21, al
   mov al, 0x28
   out 0xA1, al
   mov al, 0x04
   out 0x21, al
   mov al, 0x02
   out 0xA1, al
   mov al, 0x01
   out 0x21, al
   out 0xA1, al
   mov al, 0xFD
   out 0x21, al
   mov al, 0xFF
   out 0xA1, al
   mov rax, ksnc
   mov rdi, 0x80420
   mov [rdi], ax
   shr rax, 16
   mov word [rdi + 2], CODE67_SEG
   mov byte [rdi + 4], 0
   mov byte [rdi + 5], 0x8E
   mov [rdi + 6], ax
   shr rax, 16
   mov [rdi + 8], eax
   mov dword [rdi + 12], 0x00000000
   mov qword [cmdc], rbx
   mov qword [relad], kniha
   lidt [idttbl]
   sti
   mov rax, savior
   mov [0], rax
tets:
   mov rdx, usert
   xor rcx, rcx
   mov cl, 0x0A
   call cpritn
   mov qword rax, [curzor]
   mov qword [cmdc], rax
testt:
   mov dl, byte [char]
   cmp dl, 0
   je testt
   cmp dl, '"'
   je upr
   cmp dl, "'"
   je dnr
   xor rax, rax
   mov qword rax, [curzor]
   cmp rax, 0xb8fa0
   ja panic
   cmp rax, 0xb8000 + 480
   jb pann
   cmp dl, "*"
   je eter
   mov byte [rax], dl
   mov byte [rax + 1], 0x07
   add qword rax, 2
   mov qword [curzor], rax
   mov byte [rax +1], 0x70
   mov byte [char], 0
   jmp testt
pann:
   mov r14, 1
   call upr
   mov rax, qword [curzor]
   cmp rax, 0xB81E0
   jb pann
   xor r14, r14
   jmp testt
panic:
   mov r14, 1
   call dnr
   mov rax, qword [curzor]
   cmp rax, 0xB8fa0
   ja panic
   xor r14, r14
   jmp testt
eter:
   mov qword rax, [curzor]
   sub rax, [cmdc]
   mov [clen], rax
   mov qword rax, [curzor]
   mov byte [rax + 1], 0x00
   xor rax, rax
   mov qword rax, [curzor]
   sub rax, 0xB8000
   xor rdx, rdx
   mov rbx, 160
   div rbx
   add rax, 1
   mul rbx
   add rax, 0xB8000
   mov qword [curzor], rax 
   mov byte [char], 0
   jmp systm
upr:
   mov rax, r8
   mov rbx, r9
   mov rcx, r10
   mov rdx, r11
   mov rsi, (0xB8000 + 3840)
   mov rdi, mbf
   xor rcx, rcx
   xor rdx, rdx
copyp:
   add rdx, 1
   mov cl, [rsi]
   mov [rdi], cl
   add rsi, 1
   add rdi, 1
   cmp rdx, 160
   jne copyp
shifter:
   mov rsi, (0xB8000 + 4000)
   mov rdi, (0xB8000 + 3840)
   xor rdx, rdx
   xor rcx, rcx
shiflp:
   add rdx, 1
   mov cl, [rdi]
   mov [rsi], cl
   sub rdi, 1
   sub rsi, 1
   cmp rdx, 3520
   jne shiflp
fsmv:
   mov rsi, [relad]
   mov rdi, (0xB8000 + 480)
   sub rsi, 160
   mov [relad], rsi
   xor rdx, rdx
   xor rcx, rcx
fsmlp:
   add rdx, 1
   mov cl, [rsi]
   mov [rdi], cl
   add rsi, 1
   add rdi, 1
   cmp rdx, 160
   jne fsmlp
   mov rsi, mbf
   mov rdi, [relad]
   xor rdx, rdx
   xor rcx, rcx
savlp:
   add rdx, 1
   mov cl, [rsi]
   mov [rdi], cl
   add rdi, 1
   add rsi, 1
   cmp rdx, 160
   jne savlp
   mov byte [char], 0
   mov qword rax, [cmdc]
   mov qword rbx, [curzor]
   add rax, 160
   add rbx, 160
   mov qword [cmdc], rax
   mov qword [curzor], rbx
   cmp r14, 1
   je cal
   mov byte [char], 0
   jmp testt
cal:
   ret
dnr:
   mov rsi, (0xB8000 + 480)
   mov rdi, mbf
   xor rcx, rcx
   xor rdx, rdx
cplp:
   add rdx, 1
   mov cl, [rsi]
   mov [rdi], cl
   add rsi, 1
   add rdi, 1
   cmp rdx, 160
   jne cplp
sfts:
   mov rsi, (0xB8000 + 640)
   mov rdi, (0xB8000 + 480)
   xor rdx, rdx
   xor rcx, rcx
sftlp:
   add rdx, 1
   mov cl, [rsi]
   mov [rdi], cl
   add rdi, 1
   add rsi, 1
   cmp rdx, 3520
   jne sftlp
lstmv:
   mov rsi, [relad]
   mov rdi, (0xB8000 + 3840)
   xor rdx, rdx
   xor rcx, rcx
lstlp:
   add rdx, 1
   mov cl, [rsi]
   mov [rdi], cl
   add rsi, 1
   add rdi, 1
   cmp rdx, 160
   jne lstlp
   mov rsi, mbf
   mov rdi, [relad]
   xor rdx, rdx
   xor rcx, rcx
svlp:
   add rdx, 1
   mov cl, [rsi]
   mov [rdi], cl
   add rdi, 1
   add rsi, 1
   cmp rdx, 160
   jne svlp
   mov qword [relad], rdi
   mov qword rax, [cmdc]
   mov qword rbx, [curzor]
   sub rax, 160
   sub rbx, 160
   mov qword [cmdc], rax
   mov qword [curzor], rbx
   mov byte [char], 0
   cmp r14, 1
   je col
   jmp testt
col:
   ret
gatherer:
   xor r10, r10
   xor rax, rax
   mov rbx, [cmdc]
   add rbx, [clen]
   cmp byte [rdx + 2], 'x'
   je itshex
   cmp byte [rdx + 2], 'X'
   je itshex
   mov rcx, 10
   xor r8, r8
   xor r9, r9
getlp:
   cmp rdx, rbx
   je konex
   movzx r8, byte [rdx]
   cmp r8, ' '
   je andr
   cmp r8, 48
   jb sike
   cmp r8, 57
   ja sike
   sub r8, 48
   imul rax, rcx
   add rax, r8
   add rdx, 2
   jmp getlp
konex:
   ret
sike:
   xor r9, r9
   mov r9, 1
   ret
itshex:
   xor r9, r9
   add rdx, 4
   mov rcx, 16
   mov r10, 1
gtlp:
   cmp rdx, rbx
   je konex
   movzx r8, byte [rdx]
   cmp r8, ' '
   je andr
   cmp r8, 48
   jb sike
   cmp r8, 102
   ja sike
   cmp r8, 'a'
   je hea
   cmp r8, 'A'
   je hea
   cmp r8, 'b'
   je heb
   cmp r8, 'B'
   je heb
   cmp r8, 'c'
   je hec
   cmp r8, 'C'
   je hec
   cmp r8, 'd'
   je hed
   cmp r8, 'D'
   je hed
   cmp r8, 'e'
   je hee
   cmp r8, 'E'
   je hee
   cmp r8, 'f'
   je hef
   cmp r8, 'F'
   je hef
   cmp r8, 57
   ja sike
   sub r8, 48
reter:
   imul rax, rcx
   add rax, r8
   add rdx, 2
   jmp gtlp
hea:
   mov r8, 10
   jmp reter
heb:
   mov r8, 11
   jmp reter
hec:
   mov r8, 12
   jmp reter
hed:
   mov r8, 13
   jmp reter
hee:
   mov r8, 14
   jmp reter
hef:
   mov r8, 15
   jmp reter
andr:
   mov r9, 2
   ret
tim:
   xor r9, r9
   mov al, 0x0a
   out 0x70, al
   in al, 0x71
   test al, 0x80
   jnz tim
   mov al, 0xb
   out 0x70, al
   in al, 0x71
   test al, 0x4
   jnz binry
   mov r9, 1
   binry:
   xor rax, rax
   xor rbx, rbx
   xor rcx, rcx
   xor rdx, rdx
   mov al, 0x7
   out 0x70, al
   in al, 0x71
   mov dl, al
   mov al, 0x4
   out 0x70, al
   in al, 0x71
   mov cl, al
   mov al, 0x2
   out 0x70, al
   in al, 0x71
   mov bl, al
   mov al, 0
   out 0x70, al
   in al, 0x71
   ret
systm:
   mov rdx, syst
   mov cl, 0xC6
   call cpritn
   mov rbx, clscm
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je clsdo
   mov rbx, hlpcm
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je helpdo
   mov rbx, vercm
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je verdo
   mov rbx, wht
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je whtdo
   mov rbx, cred
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je creddo
   mov rbx, echcm
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je echdo
   mov rbx, memf
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je memfdo
   mov rbx, tran
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je trando
   mov rbx, memp
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je mmpdo
   mov rbx, time
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je tmdo
   mov rbx, wdr
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je debdo
   mov rbx, stai
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je stdo
   mov rbx, reds
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je rdsdo
   mov rbx, inout
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je ido
   mov rbx, pltai
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je pltdo
   mov rbx, asii
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je qdo
   mov rbx, wakup
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je prd
   mov rbx, dhd
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je sgmd
   mov rbx, dht
   mov rax, [cmdc]
   call cmper
   cmp rax, 1
   je sgmt
newliner:
   xor rax, rax
   mov qword rax, [curzor]
   sub rax, 0xB8000
   xor rdx, rdx
   mov rbx, 160
   div rbx
   add rax, 1
   mul rbx
   add rax, 0xB8000
   mov qword [curzor], rax
   jmp tets
dhd: db 'testcmd1', 0
dht: db 'testcmd2', 0
sgmt:
sgmd:
   mov rax, pps
   mov rbx, logits
   mov r9, 1   
   call armx
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   jmp newliner
qdo:
   mov rbx, [cmdc]
   add rbx, 12
   mov rax, [cmdc]
   add rax, [clen]
   mov rdx, chata
   mov qword [bufc], rdx
   call wavef
   call tkenc
   mov rdx, tkid
prtklp:
   cmp rdx, qword [tkcz]
   jae neowliner
   mov rax, qword [rdx]
   push rdx
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, med
   mov cl, 0x7
   call cpritn
   pop rdx
   add rdx, 8
   jmp prtklp
neowliner:
   mov rax, qword [tkcz]
   mov rbx, tkid
   sub rax, rbx
   mov rbx, 8
   xor rdx, rdx
   div rbx
   mov qword [tks], rax
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   cmp dword [bsadb], 0
   je nbs
   mov eax, dword [bosb]
   mov qword [bosp], rax
   mov rax, qword [tks]
   inc rax
   mov qword [tks], rax
nbs:
   mov rdi, 0x100000
   add rdi, 24
   mov qword [tens], rdi
   mov rdi, rts1
   mov rsi, rtz
   call etfun
   mov rdi, rts2
   mov rsi, rds
   call etfun
   mov rdi, rts3
   mov rsi, rst
   call etfun
   mov rax, 0x100000
   add rax, [smollm2_gguf_hdr_len]
   add rax, [smollm2_token_embd_off]
   mov [embd_data_ptr], rax
   mov rax, 0x100000
   add rax, [smollm2_gguf_hdr_len]
   add rax, [smollm2_output_norm_off]
   mov [output_norm_ptr], rax
   mov rbx, dum
   call fcon
   movd eax, xmm0
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov rbx, dmby
   call fcon
   movd eax, xmm0
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov rbx, testval
   call fcon
   movd eax, xmm0
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov byte [qantizat], 0
   mov rbx, tdf
   call fcon
   movd eax, xmm0
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rax, [toks]
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov rax, [toke]
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov eax, dword [emblb]
   mov rcx, tkid
   mov rbx, qword [rcx]
   xor rdx, rdx
   mul rbx
   shl rax, 1
   add rax, [embd_data_ptr]
   mov rbx, rax
   xor rax, rax
   mov rcx, pps
   call fslp
   jmp hbf
fslp:
   cmp rax, 576
   je nofs
   push rbx
   push rax
   push rcx
   call fcon
   pop rcx
   movss dword [rcx], xmm0
   pop rax
   pop rbx
   inc rax
   add rbx, 2
   add rcx, 4
   jmp fslp
nofs:
   ret
ppcp:
   cmp rdx, 576
   je rt
   mov al, byte [rcx]
   mov byte [rbx], al
   inc rcx
   inc rbx
   inc rdx
   jmp ppcp
rt: 
   ret
moj:
   mov rsi, [output_ptr]
   mov rdi, logits
   mov r15d, dword [vszb]
   xorps xmm3, xmm3
   mov r14d, dword [emblb]
   mov r13, pps
ij:
   mov rbx, rsi
   call fcon
   mulss xmm0, dword [r13]
   addss xmm3, xmm0
   add rsi, 2
   add r13, 4
   dec r14
   cmp r14, 0
   jnz ij
   dec r15
   movss dword [rdi], xmm3
   add rdi, 4
   cmp r15, 0
   jnz usj
   ret
usj:
   xorps xmm3, xmm3
   mov r14d, dword [emblb]
   mov r13, pps
   jmp ij
hbf:
   xor rdx, rdx
   mov rcx, pps
   mov rbx, sj
   call ppcp
   mov rdi, smlts
   add rdi, 2088
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   sub rdi, 144
   call fun
   mov rdi, [output_norm_ptr]
   call rmsn
   call moj
   mov rbx, logits
   call armx
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   jmp newliner
pps: times 576 dq 0
fun:
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   push rdi
   mov rbx, qword [rdi]
   add rbx, 0x100000
   mov rcx, suppa
   xor rax, rax
   call fslp
   mov rdi, suppa
   call rmsn
   pop rdi
   add rdi, 8
   ret
tlop:
   push rax
   push rbx
   push rcx
   push rdx
   push rsi
   push rdi
   push r8
   push r9
   push r10
   push r11
   push r12
   push r13
   push r14
   push r15
   push rbp
   mov rdi, 0x80000
   mov rcx, 270
   xor rax, rax
   rep stosq
   xor r12, r12
tlouter:
   cmp r12, 30
   jae tldone
   mov rax, r12
   call tlputdec
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enfa
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip0
   add r8, 0x100000
   add r8, [smollm2_gguf_hdr_len]
   mov [0x80000 + r12*8], r8
tlskip0:
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enfb
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip1
   add r8, 0x100000
   add r8,[smollm2_gguf_hdr_len]
   mov [0x800F0 + r12*8], r8
tlskip1:
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enfc
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip2
   add r8, 0x100000
   add r8,[smollm2_gguf_hdr_len]
   mov [0x801E0 + r12*8], r8
tlskip2:
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enfd
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip3
   add r8, 0x100000
   add r8,[smollm2_gguf_hdr_len]
   mov [0x802D0 + r12*8], r8
tlskip3:
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enfe
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip4
   add r8, 0x100000
   add r8,[smollm2_gguf_hdr_len]
   mov [0x803C0 + r12*8], r8
tlskip4:
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enff
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip5
   add r8, 0x100000
   add r8,[smollm2_gguf_hdr_len]
   mov [0x804B0 + r12*8], r8
tlskip5:
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enfg
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip6
   add r8, 0x100000
   add r8,[smollm2_gguf_hdr_len]
   mov [0x805A0 + r12*8], r8
tlskip6:
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enfh
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip7
   add r8, 0x100000
   add r8,[smollm2_gguf_hdr_len]
   mov [0x80690 + r12*8], r8
tlskip7:
   mov rdi, name_buf
   mov rsi, blkp
   call prcp
   mov rsi, ldig
   call prcp
   mov al, '.'
   stosb
   mov rsi, enfi
   call prcp
   mov byte [rdi], 0
   mov rdi, name_buf
   call etfun
   cmp r8, 0
   je tlskip8
   add r8, 0x100000
   add r8,[smollm2_gguf_hdr_len]
   mov [0x80780 + r12*8], r8
tlskip8:
   inc r12
   jmp tlouter
tldone:
   mov rdx, tldone_msg
   mov cl, 0x7
   call cpritn
   pop rbp
   pop r15
   pop r14
   pop r13
   pop r12
   pop r11
   pop r10
   pop r9
   pop r8
   pop rdi
   pop rsi
   pop rdx
   pop rcx
   pop rbx
   pop rax
   jmp newliner
tldone_msg: db 'layers set', 0
tlputdec:
   push rbx
   push rdi
   mov rdi, ldig
   xor rdx, rdx
   mov rcx, 10
   div rcx
   test rax, rax
   jz tlpdone1
   add al, '0'
   stosb
tlpdone1:
   mov al, dl
   add al, '0'
   stosb
   xor al, al
   stosb
   pop rdi
   pop rbx
   ret
armx:
   mov dword [sml], 0xff800000
   mov qword [psp], 0
   xor r9, r9
amxlp:
   cmp r9, 49152
   jae amxdone
   movss xmm0, dword [rbx + r9*4]
   movss xmm1, dword [sml]
   ucomiss xmm0, xmm1
   jbe amxno
   movss dword [sml], xmm0
   mov qword [psp], r9
amxno:
   inc r9
   jmp amxlp
amxdone:
   mov rax, qword [psp]
   ret
psp: dq 0
sml: dd 0xff800000
rmsn:
   xorps xmm0, xmm0
   mov rcx, 576
   mov rbx, pps
   xor rdx, rdx
   push rdi
proot:
   movss xmm1, dword [rbx]
   mulss xmm1, xmm1
   addss xmm0, xmm1
   add rbx, 4
   inc rdx
   cmp rcx, rdx
   jne proot
   cvtsi2ss xmm1, rcx
   divss xmm0, xmm1
   addss xmm0, dword [lneb]
   sqrtss xmm0, xmm0
   movss xmm1, xmm0
   movss xmm0, dword [tdf]
   divss xmm0, xmm1
   mov rbx, pps
   mov rax, rbx
   xor rdx, rdx
   pop rdi
secp:
   movss xmm2, dword [rbx]
   mulss xmm2, xmm0
   movss xmm1, dword [rdi]
   mulss xmm2, xmm1
   movss dword [rax], xmm2
   add rbx, 4
   add rax, 4
   add rdi, 4
   inc rdx
   cmp rdx, rcx
   jne secp
   ret
name_buf: times 32 db 0
ldig: times 4 db 0
blkp: db 'blk.', 0
dotw: db '.weight', 0
hd: dq 0
tenfp: db 'blk.', 0
tenf: db 22
tdf: dd 0x3F800000
output_norm_ptr: dq 0
embd_data_ptr: dq 0
dq: dq 0
qentizat: dq 1
dum: dq 0
enfa: db "attn_norm.weight", 0
enfb: db "attn_q.weight", 0
enfc: db "attn_k.weight", 0
enfd: db "attn_v.weight", 0
enfe: db "attn_output.weight", 0
enff: db "ffn_norm.weight", 0
enfg: db "ffn_gate.weight", 0
enfh: db "ffn_up.weight", 0
enfi: db "ffn_down.weight", 0
output_ptr: dq 0x002B3F40
qantizat: db 1
testval: dw 0x3C00
dmby: dw 0x7C00
debil: db '-#7€(€;€8€!€-€8€;€+€€8#+#86"6"63+4;*7ueuehrbsuienek', 0
prcp:
   mov al, byte [rsi]
   cmp al, 0
   je pre
   mov byte [rdi], al
   inc rsi
   inc rdi
   jmp prcp
pre:
   ret
fcon:
   cmp byte [qantizat], 0
   je skpf
   movzx eax, word [rbx]
   cmp ax, 0
   je zerf
   mov cx, ax
   not cx
   test cx, 0111110000000000b
   jz infin
   mov ebx, eax
   mov edx, eax
   mov ecx, 0111110000000000b
   and eax, ecx
   shr eax, 10
   add eax, 112
   shl eax, 23
   mov ecx, 0000001111111111b
   and edx, ecx
   shl edx, 13
   mov ecx, 1000000000000000b
   and ebx, ecx
   shl ebx, 16
   or eax, edx
   or eax, ebx
   movd xmm0, eax
   ret
skpf:
   mov eax, dword [rbx]
   movss xmm0, [rbx]
   ret
zerf:
   xor eax, eax
   movd xmm0, eax
   ret
infin:
   mov ecx, eax
   and ecx, 1000000000000000b
   shl ecx, 16
   mov eax, 0x7F800000
   or eax, ecx
   movd xmm0, eax
   ret
wavef:
   xor rcx, rcx
wordelizer:
   cmp rax, rbx
   je endchk
   cmp byte [rbx], ' '
   je dskp
   inc rcx
   add rbx, 2
   jmp wordelizer
endchk:
   cmp rcx, 0
   je formated
   call flushword
   ret
tks: dq 0
dskp:
   cmp rcx, 0
   je gut
   push rax
   push rbx
   call flushword
   pop rbx
   pop rax
   add rbx, 2
   jmp wordelizer
gut:
   add rbx, 2
   jmp wordelizer
formated:
   ret
flushword:
   mov rdx, qword [bufc]
   mov byte [rdx], cl
   add rdx, 1
   mov r9, rcx
   shl r9, 1
   sub rbx, r9
   xor rax, rax
   mov r8, rdx
pplp:
   cmp rcx, rax
   je idn
   mov dl, byte [rbx]
   mov byte [r8], dl
   add rbx, 2
   inc rax
   inc r8
   jmp pplp
idn:
   mov qword [bufc], r8
   xor rcx, rcx
   ret
bufc: dq 0
ido:
   mov rdx, [cmdc]
   add rdx, 12
   call gatherer
   mov r15, rax
   push r15
   cmp r9, 1
   je sike
   add rdx, 2
   call gatherer
   cmp r9, 1
   je sike
   mov r15, rax
   push r15
   cmp r9, 1
   je sike
   add rdx, 2
   call gatherer
   cmp r9, 1
   je sike
   mov r14, rax
   pop r15
   mov r13, r15
   pop r15
   mov rax, r15
   xor rdx, rdx
   mov rdx, r13
   out dx, al
   mov rdx, r14
   in al, dx
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   jmp newliner
tmdo:
   call tim
   cmp r9, 0
   je bint
   xor r13, r13
   xor r12, r12
   xor r11, r11
   mov r11, rbx
   mov r12, rcx
   mov r13, rdx
   call hexbintrn
   mov rdx, netanyahu
   add rdx, 16
   mov cl, 0x7
   call cpritn
   mov rdx, crk
   mov cl, 0x7
   call cpritn
   mov rax, r11
   call hexbintrn
   mov rdx, netanyahu
   add rdx, 16
   mov cl, 0x7
   call cpritn
   mov rdx, crk
   mov cl, 0x7
   call cpritn
   mov rax, r12
   call hexbintrn
   mov rdx, netanyahu
   add rdx, 16
   mov cl, 0x7
   call cpritn
   mov rdx, crk
   mov cl, 0x7
   call cpritn
   mov rax, r13
   call hexbintrn
   mov rdx, netanyahu
   add rdx, 16
   mov cl, 0x7
   call cpritn
   jmp newliner
stdo:
   mov rsi, 0x100000
   cmp dword [rsi], 'GGUF'
   jne rrr
   add rsi, 4
   xor rax, rax
   mov eax, dword [rsi]
   mov dword [verison], eax
   add rsi, 4
   mov rax, qword [rsi]
   mov qword [tentry], rax
   add rsi, 8
   mov rax, qword [rsi]
   mov qword [mentry], rax
   add rsi, 8
   push rsi
   mov rdx, vtxt
   mov cl, 0x7
   call cpritn
   mov eax, dword [verison]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, mtxt
   mov cl, 0x7
   call cpritn
   mov rax, qword [mentry]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, ttet
   mov cl, 0x7
   call cpritn
   mov rax, qword [tentry]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rbx, (0x100000 + 24)
genlp:
   mov rax, qword [rbx]
   add rbx, 8
   cmp eax, 20
   je findmtdt
tie:
   add rbx, rax
   mov eax, dword [rbx]
   call tagid
   cmp r9, 1
   je parseerr
   add rbx, 4
   add rbx, rdx
   jmp genlp
findmtdt:
   push rax
   push rbx
   push rcx
   push rdx
   mov rax, rbx
   mov rbx, gnar
   call cmp
   cmp rax, 1
   je ganok
   pop rdx
   pop rcx
   pop rbx
   pop rax
   jmp tie
ganok:
   pop rdx
   pop rcx
   pop rbx
   pop rax
   add rbx, 20
   mov eax, dword [rbx]
   call tagid
   sub rdx, 8
   add rbx, 12
   mov rdi, prbf
   mov qword [bflen], rdx
cper:
   cmp rdx, 64
   jae parseerr
   mov al, byte [rbx]
   mov byte [rdi], al
   add rbx, 1
   add rdi, 1
   sub rdx, 1
   cmp rdx, 0
   jne cper
   mov rdx, gentxt
   mov cl, 0x7
   call cpritn
   mov rdx, prbf
   mov cl, 0x7
   call cpritn
   mov rdx, mtext
   mov cl, 0x7
   call cpritn
   mov rdi, bos
   mov rsi, bosb
   call mdfun
   mov rdi, eos
   mov rsi, eosb
   call mdfun
   mov rdi, tok
   call mdfun
   sub r8, 0x100000
   sub r9, 0x100000
   mov qword [toks], r8
   mov qword [toke], r9
   mov rdi, scr
   call mdfun
   mov qword [scrs], r8
   mov qword [scre], r9
   mov rdi, tty
   call mdfun
   mov qword [ttys], r8
   mov qword [ttye], r9
   mov rsi, vsz
   call spcp
   mov rdi, prbf
   mov rsi, vszb
   call mdfun
   mov rsi, blc
   call spcp
   mov rdi, prbf
   mov rsi, blcb
   call mdfun
   mov rsi, ctl
   call spcp
   mov rdi, prbf
   mov rsi, ctlb
   call mdfun
   mov rsi, rpf
   call spcp
   mov rdi, prbf
   mov rsi, rpfb
   call mdfun
   mov rsi, embl
   call spcp
   mov rdi, prbf
   mov rsi, emblb
   call mdfun
   mov rsi, ffl
   call spcp
   mov rdi, prbf
   mov rsi, fflb
   call mdfun
   mov rsi, ath
   call spcp
   mov rdi, prbf
   mov rsi, athb
   call mdfun
   mov rsi, atv
   call spcp
   mov rdi, prbf
   mov rsi, atvb
   call mdfun
   mov rsi, lne
   call spcp
   mov rdi, prbf
   mov rsi, lneb
   call mdfun
   mov eax, [blcb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [emblb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [athb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [atvb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [fflb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [vszb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [lneb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [bosb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [eosb]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [toks]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [toke]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [scrs]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [scre]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [ttys]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, pipe
   mov cl, 0x7
   call cpritn
   mov eax, [ttye]
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   jmp newliner
vtxt: db 'version: ', 0
mtext: db ' going trough metadata entryes..... ', 0
mtxt: db ' number of metadata entryes found: ', 0
ttet: db ' number of tensor entryes found: ', 0
verison: dd 0
pipe: db '|', 0
mentry: dq 0
tentry: dq 0
gentxt: db ' genneral architecture: ', 0
mdnf: db 'model was mot found', 0
bflen: dq 0
prbf: times 64 db 0
blc:  db '.block_count', 0
blcb: dd 30
embl: db '.embedding_length', 0
emblb: dd 576
ath:  db '.attention.head_count', 0
athb: dd 9
atv:  db '.attention.head_count_kv', 0
atvb: dd 3
ctl:  db '.context_length', 0
ctlb: dd 8192
ffl:  db '.feed_forward_length', 0
fflb: dd 1536
vsz:  db '.vocab_size', 0
vszb: dd 49152
rpf:  db '.rope.freq_base', 0
rpfb: dd 100000
lne:  db '.attention.layer_norm_rms_epsilon', 0
lneb: dd 0x38d1b717
bsad: dd 'tokenizer.ggml.add_bos_token', 0
bsadb: dd 1
bos:  db 'tokenizer.ggml.bos_token_id', 0
bosb: dd 1
eos:  db 'tokenizer.ggml.eos_token_id', 0
eosb: dd 2
tok:  db 'tokenizer.ggml.tokens', 0
toks: dq 0x00000447
toke: dq 0x000BA665
scr:  db 'tokenizer.ggml.scores', 0
scrs: dq 0
scre: dq 0
tty:  db 'tokenizer.ggml.token_type', 0
ttys: dq 0x000BA68A
ttye: dq 0x000EA696
mrg:  db 'tokenizer.ggml.merges', 0
mrgs: dq 0x000EA6B7
mrge: dq 0x001AFDA8
rts1: db 'token_embd.weight', 0
rtz: dq 0 
rts2: db 'output_norm.weight', 0
rds: dq 0
rts3: db 'output.weight', 0
rst: dq 0
smollm2_arch: db 'llama', 0
smollm2_name: db 'SmolLM2-135M-Instruct-f16', 0
smollm2_gguf_hdr_len: dq 0x1B3F40
smollm2_token_embd_off: dq 0x00000000
smollm2_output_norm_off: dq 0x100A1C00
smollm2_blk_base_off: dq 0x03600000
smollm2_blk_stride: dq 0x006C1200
smollm2_tok_start_off: dq 0x00000447
smollm2tokstartoff: dq 0x00000447
smollm2_tok_end_off: dq 0x000BA665
smollm2_tty_start_off: dq 0x000BA68A
smollm2_tty_end_off: dq 0x000EA696
smollm2_mrg_start_off: dq 0x000EA6B7
smollm2_mrg_end_off: dq 0x001AFDA8
smollm2_attn_norm_rel: dq 0x0
smollm2_ffn_down_rel: dq 0x00000900
smollm2_ffn_gate_rel: dq 0x001B0900
smollm2_ffn_up_rel: dq 0x00360900
smollm2_ffn_norm_rel: dq 0x00510900
smollm2_attn_k_rel: dq 0x00511200
smollm2_attn_output_rel: dq 0x00547200
smollm2_attn_q_rel: dq 0x005E9200
smollm2_attn_v_rel: dq 0x0068B200
tdec: ; rax = token id, rbx start of id book, xor rdx
   cmp rdx, rax
   je cmplt
   mov rdi, qword [rbx]
   add rbx, rdi
   add rbx, 8
   inc rdx
   jmp tdec
cmplt:
   mov rax, qword [rbx]
   add rbx, 8
   ret
tcmp:
   cmp rax, 0
   je shortword
   cmp rdi, 0
   je yestok
   mov dl, byte [rbx]
   cmp dl, byte [rcx]
   jne notok
   inc rbx
   inc rcx
   dec rax
   jmp tcmp
shortword:
   cmp rdi, 0
   je yestok
   mov rdx, 1
   ret
notok:
   mov rdx, 1
   ret
yestok:
   xor rdx, rdx
   ret
tkenc:
   mov qword [tkcz], tkid
   mov rbx, chata
wordloop:
   cmp rbx, qword [bufc]
   jae tkdone
   mov rcx, [toks]
   add rcx, 0x100000
   xor rsi, rsi
enlp:
   xor rax, rax
   mov al, byte [rbx]
   movzx edi, byte [rcx]
   cmp rdi, rax
   je try
notr:
   add rcx, rdi
   add rcx, 4
   inc rsi
   mov eax, dword [vszb]
   cmp esi, eax
   je nofind
   jmp enlp
try:
   push rax
   push rbx
   push rcx
   push rdi
   push rsi
   add rcx, 4
   call tcmp
   pop rsi
   pop rdi
   pop rcx
   pop rbx
   pop rax
   cmp rdx, 1
   je notr
   mov rdx, qword [tkcz]
   mov qword [rdx], rsi
   add rdx, 8
   mov qword [tkcz], rdx
   inc rbx
   jmp wordloop
nofind:
   xor rax, rax
   mov al, byte [rbx]
   add rbx, 1
   jmp wordloop
tkdone:
   ret
tkcz: dq 0
bosp: dq 0
tkid: times 256 dq 0
spcp:
   mov rax, qword [bflen]
   mov rbx, prbf
   add rbx, rax
splp:
   mov al, byte [rsi]
   mov byte [rbx], al
   cmp al, 0
   je spend
   inc rbx
   inc rsi
   jmp splp
spend:
   ret
rrr:
   mov rdx, mdnf
   mov cl, 0x7
   call cpritn
   jmp newliner
tens: dq 0
etfun:
   push r12
   mov rbx, qword [tens]
   mov r11, qword [mentry]
mtskiplp:
   cmp r11, 0
   je tenset
   mov rax, qword [rbx]
   add rbx, 8
   add rbx, rax
   mov eax, dword [rbx]
   call tagid
   cmp r9, 1
   je parseerr
   add rbx, 4
   add rbx, rdx
   sub r11, 1
   jmp mtskiplp
   tenset:
   mov r10, qword [tentry]
etflp:
   cmp r10, 0
   je etfl
   push rax
   push rbx
   push rcx
   push rdx
   push rsi
   push rdi
   mov rax, r10
   call hexbintrn
   pop rdi
   pop rsi
   pop rdx
   pop rcx
   pop rbx
   pop rax
   sub r10, 1
   mov rax, qword [rbx]
   add rbx, 8
   push rdi
   push rsi
   push rax
   push rbx
   push rcx
   push rdx
   push r10
   mov rax, rbx
   mov rbx, rdi
   call cmp
   cmp rax, 1
   jne ntsok
   pop r10
   pop rdx
   pop rcx
   pop rbx
   pop rax
   pop rsi
   pop rdi
   Add rbx, rax
   mov eax, dword [rbx]
   mov r13d, eax
   mov r12, 1
   xor rcx, rcx
lendim:
   cmp ecx, r13d
   jae lendone
   mov rax, qword [rbx + 4 + rcx*8]
   imul r12, rax
   inc ecx
   jmp lendim
lendone:
   mov ecx, r13d
   lea rcx, [rcx*8 + 4]
   mov eax, dword [rbx + rcx]
   cmp eax, 0
   jne lendif16
   shl r12, 2
   jmp lendmuldone
lendif16:
   shl r12, 1
lendmuldone:
   lea rbx, [rbx + 4 + r13*8]
   mov eax, dword [rbx]
   add rbx, 4
   mov rcx, qword [rbx]
   add rbx, 8
   mov r8, rcx
   mov r9, 0
   pop r12
   ret
ntsok:
   pop r10
   pop rdx
   pop rcx
   pop rbx
   pop rax
   pop rsi
   pop rdi
   add rbx, rax
   mov eax, dword [rbx]
   lea rbx, [rbx + 4 + rax*8]
   add rbx, 4
   add rbx, 8
   jmp etflp
etfl:
   mov rdi, r9
   xor r8, r8
   xor r9, r9
   xor r12, r12
   pop r12
   ret
mdfl:
   mov rdi, r9
   xor r8, r8
   xor r9, r9
   ret
parseerr:
   mov rdx, badtag
   mov cl, 0x7
   call cpritn
   pop r12
   jmp newliner
mdfun:
  mov rbx, 0x100000
  add rbx, 24
  mov r10, qword [mentry]
mdlp:
  cmp r10, 0
  je mdfl
  push rax
  push rbx
  push rcx
  push rdx
  push rsi
  push rdi
  pop rdi
  pop rsi
  pop rdx
  pop rcx
  pop rbx
  pop rax
  sub r10, 1
  mov rax, qword [rbx]
  add rbx, 8
  push rdi
  push rsi
  push rax
  push rbx
  push rcx
  push rdx
  push r10
  mov rax, rbx
  mov rbx, rdi
  call cmp
  cmp rax, 1
  jne mdskip
  pop r10
  pop rdx
  pop rcx
  pop rbx
  pop rax
  pop rsi
  pop rdi
  add rbx, rax
  mov eax, dword [rbx]
  call tagid
  cmp r9, 1
  je parseerr
  add rbx, 4
  mov r8, rbx
  add rbx, rdx
  mov r9, rbx
  ret
mdskip:
  pop r10
  pop rdx
  pop rcx
  pop rbx
  pop rax
  pop rsi
  pop rdi
  add rbx, rax
  mov eax, dword [rbx]
  call tagid
  cmp r9, 1
  je parseerr
  add rbx, 4
  add rbx, rdx
  jmp mdlp
badtag: db 'bad tag', 0
tagid:
   xor r9, r9
   cmp eax, 0
   je ui8
   cmp eax, 1
   je ui8
   cmp eax, 2
   je ui6
   cmp eax, 3
   je ui6
   cmp eax, 4
   je ui2
   cmp eax, 5
   je ui2
   cmp eax, 6
   je ui2
   cmp eax, 7
   je ui8
   cmp eax, 8
   je strign
   cmp eax, 9
   je array
   cmp eax, 10
   je ui4
   cmp eax, 11
   je ui4
   cmp eax, 12
   je ui4
   xor rdx, rdx
   mov r9, 1
   ret
ui8:
   xor rdx, rdx
   mov rdx, 1
   ret
ui6:
   xor rdx, rdx
   mov rdx, 2
   ret
ui2:
   xor rdx, rdx
   mov rdx, 4
   ret
ui4:
   xor rdx, rdx
   mov rdx, 8
   ret
strign:
   push rbx
   mov rdx, qword [rbx + 4]
   add rdx, 8
   pop rbx
   ret
array:
   push rbx
   add rbx, 4
   mov eax, dword [rbx]
   add rbx, 4
   mov rcx, qword [rbx]
   add rbx, 8
   cmp eax, 8
   je arrstr
   push rcx
   call tagid
   pop rcx
   mov rax, rcx
   mul rdx
   add rax, 12
   mov rdx, rax
   pop rbx
   ret
arrstr:
   push r11
   xor r11, r11
arrstrlp:
   cmp rcx, 0
   je arrstrdone
   mov rax, qword [rbx]
   add rax, 8
   add r11, rax
   add rbx, rax
   dec rcx
   jmp arrstrlp
arrstrdone:
   add r11, 12
   mov rdx, r11
   pop r11
   pop rbx
   ret
bint:
   xor r11, r11
   xor r12, r12
   xor r13, r13
   mov r11, rbx
   mov r12, rcx
   mov r13, rdx
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rax, r11
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, crk
   mov cl, 0x7
   call cpritn
   mov rax, r12
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, crk
   mov cl, 0x7
   call cpritn
   mov rax, r13
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   jmp newliner
clsdo:
   call cls
   jmp tets
rdsdo:
   mov rdx, [cmdc]
   add rdx, 10
   call gatherer
   call drs
   xor r14, r14
   mov r14, 0x7b000
dtp:
   movzx rax, byte [r14]
   call bintrn
   push r14
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, medz
   mov cl, 0x7
   call cpritn
   pop r14
   add r14, 1
   cmp r14, 0x7b200
   je newliner
   jmp dtp
medz: db ' ', 0
helpdo:
   mov rdx, hlptxt
   mov cl, 0x07
   call cpritn
   jmp tets
verdo:
   mov rdx, vertxt
   mov cl, 0x07
   call cpritn
   jmp tets
creddo:
   mov rdx, credtxt
   mov cl, 0x07
   call cpritn
   jmp tets
mmpdo:
   mov rdx, [cmdc]
   add rdx, 8
   call gatherer
   cmp r9, 2
   jb sike
   add rdx, 2
   mov r15, rax
   call gatherer
   cmp r9, 1
   je sike
   mov [r15], rax
   mov cl, 0x07
   mov rdx, done
   call cpritn
   jmp newliner
echdo:
   mov rax, [cmdc]
   add rax, 16
   mov rax, [clen]
   xor rdx, rdx
   mov rcx, 2
   div rcx
   mov rdx, rax
   sub rdx, 8
   mov rax, [cmdc]
   add rax, 16
   xor rcx, rcx
   xor r8, r8
cpri:
   add r8, 1
   mov byte cl, [rax]
   mov rbx, [curzor]
   mov byte [rbx], cl
   mov byte [rbx + 1], 0x07
   cmp r8, rdx
   je newliner
   add rbx, 2
   mov [curzor], rbx
   add rax, 2
   jmp cpri
trando:
   mov rdx, [cmdc]
   add rdx, 20
   call gatherer
   cmp r9, 1
   je errr
   cmp r10, 1
   je ijex
   call hexbintrn
   jmp cller
ijex:
   call bintrn
cller:
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   jmp newliner
whtdo:
   mov rax, cr0
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka
   mov cl, 0x07
   call cpritn
   mov rax, cr2
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka2
   mov cl, 0x07
   call cpritn
   mov rax, cr3
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka3
   mov cl, 0x07
   call cpritn
   mov rax, cr4
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka4
   mov cl, 0x07
   call cpritn
   mov rax, cr8
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka8
   mov cl, 0x07
   call cpritn
   mov rax, qword [smlts]
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   jmp newliner
debdo:
   mov rax, dr0
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarkad
   mov cl, 0x07
   call cpritn
   mov rax, dr1
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka1d
   mov cl, 0x07
   call cpritn
   mov rax, dr2
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka2d
   mov cl, 0x07
   call cpritn
   mov rax, dr3
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka3d
   mov cl, 0x07
   call cpritn
   mov rax, dr6
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka6d
   mov cl, 0x07
   call cpritn
   mov rax, dr7
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   mov rdx, ciarka7d
   mov cl, 0x07
   call cpritn
   jmp newliner
memfdo:
   mov rdx, [cmdc]
   add rdx, 10
   call gatherer
   cmp r9, 1
   je errr
   mov bl, byte [rax]
   movzx rax, byte bl
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x07
   call cpritn
   jmp newliner
errr:
   mov rdx, numer
   mov cl, 0x07
   call cpritn
   jmp newliner
prd:
   mov rdx, [cmdc]
   add rdx, 14
   call gatherer
   cmp r9, 1
   je errr
   mov r15, rax
   push r15
   xor rax, rax
   mov rax, 0xe0000
prl:
   cmp dword [rax], 'RSD '
   jne dalsi
   cmp dword [rax + 4], 'PTR '
   je konecne
dalsi:
   add rax, 16
   cmp rax, 0x100000
   jae crash
   jmp prl
dalsii:
   sub rax, 20
   jmp dalsi
dalsiy:
   sub rax, 36
   jmp dalsi
konecne:
   xor rbx, rbx
   xor rcx, rcx
   xor rdx, rdx
klp:
   movzx r10, byte [rax]
   add rbx, r10
   add rdx, 1
   add rax, 1
   cmp rdx, 20
   jne klp
   cmp bl, 0
   jne dalsii
   sub rax, 5
   cmp byte [rax], 0
   jne acpd
   xor rbx, rbx
   mov ebx, dword [rax + 1]
   xor rdx, rdx
   jmp nextwak
acpd:
   sub rax, 15
   xor rbx, rbx
   xor rdx, rdx
llp:
   movzx r10, byte [rax]
   add rbx, r10
   add rdx, 1
   add rax, 1
   cmp rdx, 36
   jne llp
   cmp bl, 0
   jne dalsiy
   xor rbx, rbx
   xor rdx, rdx
   mov ebx, dword [rax - 20]
   mov rdx, qword [rax - 12]
   jmp nextwak
nextwak:
   cmp rdx, 0
   je rads
   cmp dword [rdx],'XSDT'
   jne crash
   xor rax, rax
   mov eax, dword [rdx + 4]
   sub rax, 36
   shr rax, 3
   mov r8, rdx
   add r8, 36
xls:
   mov rcx, qword [r8]
   cmp dword [rcx], 'APIC'
   je wfound
   add r8, 8
   jmp xls
rads:
   cmp dword [rbx], 'RSDT'
   jne crash
   xor rax, rax
   mov eax, dword [rbx + 4]
   sub rax, 36
   shr rax, 2
   mov r8, rbx
   add r8, 36
rls:
   mov ecx, dword [r8]
   cmp dword [rcx], 'APIC'
   je wfound
   add r8, 4
   jmp rls
wfound:
   xor r12, r12
   mov r12, rcx
   add rcx, 4
   mov eax, dword [rcx]
   add r12, rax
   add rcx, 40
   xor r13, r13
   xor r9, r9
   xor r11, r11
   mov r11, crid
nlop:
   mov bl, byte [rcx]
   movzx rax, byte [rcx + 1]
   cmp bl, 0
   jne adfa
   mov edx, dword [rcx + 4]
   test dl, 1
   je dsbld
   mov r10, rbx
   mov bl, byte [rcx + 3]
   mov byte [r11], bl
   inc r11
   inc r13
   jmp adfa
dsbld:
   add r9, 1
adfa:
   add rcx, rax
   cmp rcx, r12
   jb nlop
   mov rcx, crid
   xor r15, r15
crcntr:
   cmp byte [rcx], 0
   je nomore
   add r15, 1
   jmp crcntr
nomore:
   mov rax, r13
   push rax
   mov rdx, fndn
   mov cl, 0x7
   call cpritn
   pop rax
   push r13
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov eax, 0x1b
   rdmsr
   shl rdx, 32
   or rax, rdx
   mov rdx, 0xFFFFFFFFFFFFF000
   and rax,rdx
   push rax
   mov eax, 1
   cpuid
   shr ebx, 24
   movzx rbx, bl
   pop rax
   mov rdx, crid
   pop r13
   pop r15
   cmp r13, r15
   jb nigggerr
   mov r13, r15
   mov r9, 1
nigggerr:
   push r13
niggger:
   cmp r13, 0
   je doner
   cmp [rdx], rbx
   je apsk
   mov cl, byte [rdx]
   movzx r10d, cl
   shl r10d, 24
   mov dword [rax + 0x310], r10d
   mov dword [rax + 0x300], 0x4500
walter:
   mov r11d, [rax + 0x300]
   test r11d, 1 << 12
   jnz walter
   push rax
   push rbx
   push rcx
   push rdx
   push r9
   in al, 0x61
   and al, 0xfc
   or al, 0x1
   out 0x61, al
   mov al, 0xb0
   out 0x43, al
   mov al, 0x9c
   out 0x42, al
   mov al, 0x2e
   out 0x42, al
until:
   in al, 0x61
   test al, 0x20
   jz until
   pop r9
   pop rdx
   pop rcx
   pop rbx
   pop rax
   mov dword [rax + 0x310], r10d
   mov dword [rax + 0x300], 0x8500
vojtech:
   mov r11d, [rax + 0x300]
   test r11d, 1 << 12
   jnz vojtech
   mov cl, byte [rdx]
   movzx r10d, cl
   shl r10d, 24
   mov dword [rax + 0x310], r10d
   mov dword [rax + 0x300], 0x4601
comoj:
   mov r11d, [rax + 0x300]
   test r11d, 1 << 12
   jnz comoj
   push rax
   push rbx
   push rcx
   push rdx
   push r9
   call tim
   in al, 0x61
   and al, 0xfc
   or al, 0x1
   out 0x61, al
   mov al, 0xb0
   out 0x43, al
   mov al, 0xEF
   out 0x42, al
   mov al, 0
   out 0x42, al
unptil:
   in al, 0x61
   test al, 0x20
   jz unptil
   pop r9
   pop rdx
   pop rcx
   pop rbx
   pop rax
   mov cl, byte [rdx]
   movzx r10d, cl
   shl r10d, 24
   mov dword [rax + 0x310], r10d
   mov dword [rax + 0x300], 0x4601
for:
   mov r11d, [rax + 0x300]
   test r11d, 1 << 12
   jnz for
   add rdx, 1
   sub r13, 1
   jmp niggger
apsk:
   cmp r9, 1
   je jomp
   sub r13, 1
jomp:
   add rdx, 1
   jmp niggger
doner:
   pop r13
   push r13
   mov rdx, crid
niglp:
   push rbx
   mov rdx, waks
   mov cl,0x7
   call cpritn
   pop rbx
   pop r13
   mov rdx, crid
   xor r8, r8
noglp:
   push rbx
   push r13
   movzx rax, byte [rdx]
   cmp rax, rbx
   je apk
   push rdx
   push r8
   call hexbintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov rdx, med
   mov cl, 0x7
   call cpritn
   pop r8
   pop rdx
   pop r13
   pop rbx
   add rdx, 1
   add r8, 1
   cmp r8, r13
   je newliner
   jmp noglp
apk:
   pop r13
   pop rbx
   add rdx, 1
   jmp noglp
crash:
   mov rdx, crtx
   mov cl, 0x7
   call cpritn
   jmp newliner
pltdo:
   mov rdx, sect
   mov cl, 0x7
   call cpritn
   mov rax, 842
   call drs
   mov rbx, 0x7b000
   cmp dword [rbx], 'GGUF'
   je gboy
   mov rdx, crtx
   mov cl, 0x7
   call cpritn
   jmp newliner
gboy:
   mov rbx, 0x100000
   mov rax, 842
gby:
   push rax
   push rbx
   call memld
   pop rbx
   pop rax
   push rax
   push rbx
   mov r15, qword [curzor]
   mov qword [czs], r15
   call bintrn
   mov rdx, netanyahu
   mov cl, 0x7
   call cpritn
   mov r15, qword [curzor]
   mov qword [cas], r15
   mov r15, qword [czs]
   mov qword [curzor], r15
   pop rbx
   pop rax
   add rbx,512
   add rax, 1
   push rax
   push rbx
   mov rbx, gend
   mov rax, 0x7b000
   call cmp
   mov r9, rax
   pop rbx
   pop rax
   cmp r9, 1
   jne gby
   mov r15, qword [cas]
   mov qword [curzor], r15
   mov rdx, med
   mov cl, 0x7
   call cpritn
   mov rdx, testx
   mov cl, 0x7
   call cpritn
   jmp newliner
memld:
   push rbx
   call drs
   pop rbx
   mov rcx, 0x7b000
   xor rdx, rdx
melp:
   mov rdi, qword [rcx]
   mov qword [rbx], rdi
   add rbx, 8
   add rcx, 8
   add rdx, 8
   cmp rdx, 512
   jne melp
   ret
crtx: db 'fart unsuccesful', 0
sect: db 'loading sector:', 0
testx: db ' succes', 0
czs: dq 0
wakup: db 'wakeup', 0
ttest: db 0
idt: db 0
shift: db 0
curzor: dq 0
pltai: db 'loadai', 0
inout: db 'inout', 0
tst: dq 0
waks: db ' succesfuly waken up aps: ', 0
char: db 0
fndn: db 'number of cores found: ', 0
idttbl:
dw 4095
dq 0x80210
head: db '+------------------------------------------------------------------------------+|                                 SofaOS X64                                   |+------------------------------------------------------------------------------+', 0
hlpcm: db 'help', 0
vercm: db 'ver', 0
cred: db 'credits', 0
hlptxt: db 'commands: help = gives u help :>ver = shows curent version of sofaoscredits = tels u who made this oscls = clears the screenwhatincr = shows u whats in cr0,cr2,cr3,cr4 and cr8saythis = echoes back what u type after ittranslate = converts hex number to decimal and backpeek = shows u whats stored at a memory addressput = puts number to a memorytime = shows timewhatindr = shows u whats in dr0,dr1,dr2,dr3,dr6 and dr7secr = reads what in disk on that sectorwakeup = wakes up some number of cores that u chose', 0
credtxt: db 'i was made by scrt401                                                ', 0
wht: db 'whatincr', 0
echcm: db 'saythis', 0
ciarka: db ' in cr0,', 0
stai: db 'setupai', 0
ciarka2: db ' in cr2,', 0
cas: dq 0
ciarka3: db ' in cr3,', 0
numer: db 'no idea what that number is', 0
ciarka4: db ' in cr4,', 0
memf: db 'peek', 0
memp: db 'put', 0
time: db 'time', 0
gnar: db 'general.architecture', 0
crk: db ':', 0
ciarka8: db ' in cr8,', 0
gend: db 'endofggufmoddelfile', 0
ccnt: dq 0
med: db ' ', 0
vertxt: db 'version 1 non ai', 0
done: db 'Done', 0
clscm: db 'cls', 0
wdr: db 'whatindr', 0
usert: db '(user)>', 0
syst: db '//system//>', 0
reds: db 'secr', 0
clen: dq 0
cmdc: dq 0
chat: db 0
ciarkad: db ' in dr0,', 0
ciarka1d: db ' in dr1,', 0
ciarka2d: db ' in dr2,', 0
ciarka3d: db ' in dr3,', 0
ciarka6d: db ' in dr6,', 0
ciarka7d: db ' in dr7', 0
tran: db 'translate', 0
asii: db 'askai', 0
relad: dq 0
smlts:
dq 0xCC4CB40
dq 0xD235D40
dq 0xD15DD40
dq 0xD2D7D40
dq 0xD193D40
dq 0xCDFD440
dq 0xCFAD440
dq 0xCC4D440
dq 0xD15D440
dq 0xC58B940
dq 0xCB74B40
dq 0xCA9CB40
dq 0xCC16B40
dq 0xCAD2B40
dq 0xC73C240
dq 0xC8EC240
dq 0xC58C240
dq 0xCA9C240
dq 0xBECA740
dq 0xC4B3940
dq 0xC3DB940
dq 0xC555940
dq 0xC411940
dq 0xC07B040
dq 0xC22B040
dq 0xBECB040
dq 0xC3DB040
dq 0xB809540
dq 0xBDF2740
dq 0xBD1A740
dq 0xBE94740
dq 0xBD50740
dq 0xB9B9E40
dq 0xBB69E40
dq 0xB809E40
dq 0xBD19E40
dq 0xB148340
dq 0xB731540
dq 0xB659540
dq 0xB7D3540
dq 0xB68F540
dq 0xB2F8C40
dq 0xB4A8C40
dq 0xB148C40
dq 0xB658C40
dq 0xAA87140
dq 0xB070340
dq 0xAF98340
dq 0xB112340
dq 0xAFCE340
dq 0xAC37A40
dq 0xADE7A40
dq 0xAA87A40
dq 0xAF97A40
dq 0xA3C5F40
dq 0xA9AF140
dq 0xA8D7140
dq 0xAA51140
dq 0xA90D140
dq 0xA576840
dq 0xA726840
dq 0xA3C6840
dq 0xA8D6840
dq 0x9D04D40
dq 0xA2EDF40
dq 0xA215F40
dq 0xA38FF40
dq 0xA24BF40
dq 0x9EB5640
dq 0xA065640
dq 0x9D05640
dq 0xA215640
dq 0x9643B40
dq 0x9C2CD40
dq 0x9B54D40
dq 0x9CCED40
dq 0x9B8AD40
dq 0x97F4440
dq 0x99A4440
dq 0x9644440
dq 0x9B54440
dq 0x8F82940
dq 0x956BB40
dq 0x9493B40
dq 0x960DB40
dq 0x94C9B40
dq 0x9133240
dq 0x92E3240
dq 0x8F83240
dq 0x9493240
dq 0x8200540
dq 0x87E9740
dq 0x8711740
dq 0x888B740
dq 0x8747740
dq 0x83B0E40
dq 0x8560E40
dq 0x8200E40
dq 0x8710E40
dq 0x7B3F340
dq 0x8128540
dq 0x8050540
dq 0x81CA540
dq 0x8086540
dq 0x7CEFC40
dq 0x7E9FC40
dq 0x7B3FC40
dq 0x804FC40
dq 0x747E140
dq 0x7A67340
dq 0x798F340
dq 0x7B09340
dq 0x79C5340
dq 0x762EA40
dq 0x77DEA40
dq 0x747EA40
dq 0x798EA40
dq 0x6DBCF40
dq 0x73A6140
dq 0x72CE140
dq 0x7448140
dq 0x7304140
dq 0x6F6D840
dq 0x711D840
dq 0x6DBD840
dq 0x72CD840
dq 0x66FBD40
dq 0x6CE4F40
dq 0x6C0CF40
dq 0x6D86F40
dq 0x6C42F40
dq 0x68AC640
dq 0x6A5C640
dq 0x66FC640
dq 0x6C0C640
dq 0x603AB40
dq 0x6623D40
dq 0x654BD40
dq 0x66C5D40
dq 0x6581D40
dq 0x61EB440
dq 0x639B440
dq 0x603B440
dq 0x654B440
dq 0x5979940
dq 0x5F62B40
dq 0x5E8AB40
dq 0x6004B40
dq 0x5EC0B40
dq 0x5B2A240
dq 0x5CDA240
dq 0x597A240
dq 0x5E8A240
dq 0x52B8740
dq 0x58A1940
dq 0x57C9940
dq 0x5943940
dq 0x57FF940
dq 0x5469040
dq 0x5619040
dq 0x52B9040
dq 0x57C9040
dq 0x4BF7540
dq 0x51E0740
dq 0x5108740
dq 0x5282740
dq 0x513E740
dq 0x4DA7E40
dq 0x4F57E40
dq 0x4BF7E40
dq 0x5107E40
dq 0x4536340
dq 0x4B1F540
dq 0x4A47540
dq 0x4BC1540
dq 0x4A7D540
dq 0x46E6C40
dq 0x4896C40
dq 0x4536C40
dq 0x4A46C40
dq 0xFB94940
dq 0x1017DB40
dq 0x100A5B40
dq 0x1021FB40
dq 0x100DBB40
dq 0xFD45240
dq 0xFEF5240
dq 0xFB95240
dq 0x100A5240
dq 0xF4D3740
dq 0xFABC940
dq 0xF9E4940
dq 0xFB5E940
dq 0xFA1A940
dq 0xF684040
dq 0xF834040
dq 0xF4D4040
dq 0xF9E4040
dq 0xEE12540
dq 0xF3FB740
dq 0xF323740
dq 0xF49D740
dq 0xF359740
dq 0xEFC2E40
dq 0xF172E40
dq 0xEE12E40
dq 0xF322E40
dq 0xE751340
dq 0xED3A540
dq 0xEC62540
dq 0xEDDC540
dq 0xEC98540
dq 0xE901C40
dq 0xEAB1C40
dq 0xE751C40
dq 0xEC61C40
dq 0xE090140
dq 0xE679340
dq 0xE5A1340
dq 0xE71B340
dq 0xE5D7340
dq 0xE240A40
dq 0xE3F0A40
dq 0xE090A40
dq 0xE5A0A40
dq 0xD9CEF40
dq 0xDFB8140
dq 0xDEE0140
dq 0xE05A140
dq 0xDF16140
dq 0xDB7F840
dq 0xDD2F840
dq 0xD9CF840
dq 0xDEDF840
dq 0xD30DD40
dq 0xD8F6F40
dq 0xD81EF40
dq 0xD998F40
dq 0xD854F40
dq 0xD4BE640
dq 0xD66E640
dq 0xD30E640
dq 0xD81E640
dq 0x88C1740
dq 0x8EAA940
dq 0x8DD2940
dq 0x8F4C940
dq 0x8E08940
dq 0x8A72040
dq 0x8C22040
dq 0x88C2040
dq 0x8DD2040
dq 0x3E75140
dq 0x445E340
dq 0x4386340
dq 0x4500340
dq 0x43BC340
dq 0x4025A40
dq 0x41D5A40
dq 0x3E75A40
dq 0x4385A40
dq 0x37B3F40
dq 0x3D9D140
dq 0x3CC5140
dq 0x3E3F140
dq 0x3CFB140
dq 0x3964840
dq 0x3B14840
dq 0x37B4840
dq 0x3CC4840
times 26112 - ($ - $$) db 0
chata: times 256 db 0
crid: times 256 db 0
kniha equ 0x900000
mbf equ 160
hlpg: times 4096 db 0
logits: times 49152 dq 0
sj: times 576 dd 0
suppa: times 576 dd 0
